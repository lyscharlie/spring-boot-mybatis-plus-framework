# DataSource配置
spring:
   jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8
   datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/lyscharlie?useUnicode=true&useSSL=false&characterEncoding=utf-8
      username: lyscharlie
      password: 2110857
      
# 端口号
server:
   context-path: /lyscharlie
   port: 8080

# 日志
logging:
   level:
      org:
         springframework: INFO
         
